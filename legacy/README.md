# legacy

Reserved for old code/systems that may require migration later.

This folder is intentionally kept separate from `archive/`:

- `archive/` = historical documentation/artifacts
- `legacy/` = executable systems/code requiring migration strategy
